class UIStore {
  - activeView: "dashboard" | "franchises" | "sites" | "interventions" | "settings" | "create" | "home"
  - viewKey: number
  + setPage(view): void
}

class FranchiseStore {
  - franchises: Franchise[]
  - selectedFranchise: Franchise | null
  - selectedSite: Site | null
  + selectFranchise(id): void
  + selectSite(id): void
  + resetSelection(): void
}

class AppLayout {
  - children: ReactNode
  + render(): JSX.Element
}

class Sidebar {
  - setPage(view): void  // via UIStore
  - selectFranchise(id): void  // via FranchiseStore
  + render(): JSX.Element
}

class MainView {
  - activeView: UIStore.activeView
  + render(): JSX.Element
}

class CreateEntryPage {
  - selectedForm: "franchise" | "site" | "intervention" | null
  + render(): JSX.Element
}

class HomePage {
  + render(): JSX.Element
}

class FranchiseService {
  + getFranchises(): Promise<Franchise[]>
  + createFranchise(data): Promise<Franchise>
}

class ElectronMain {
  - prismaClient
  + handleGetFranchises(): Franchise[]
  + handleCreateFranchise(data): Franchise
}
